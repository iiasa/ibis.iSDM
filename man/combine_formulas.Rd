% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-form.R
\name{combine_formulas}
\alias{combine_formulas}
\title{Combine or concatenate multiple formula objects}
\usage{
combine_formulas(..., combine = "both", env = parent.frame())
}
\arguments{
\item{...}{Any number \code{\link{formula}} objects in "LHS ~ RHS" format, also supporting
\code{\link{character}} strings.}

\item{combine}{\code{\link{character}} on whether LHS and RHS duplicates are to be removed. Can
be set to either \code{"lhs"}, \code{"rhs"} or \code{"both"} (Default).}

\item{env}{A new environment of the formula \code{(def=parent.frame())}.}
}
\value{
A formula as \code{cbind(lhs_1, lhs_2, ...) ~ rhs_1 + rhs_2 + ...} or
\code{lhs ~ rhs_1 + rhs_2} in case of identical LHS (see examples).
}
\description{
This small helper function allows to combine multiple \code{formula()} objects
into one. In the case of duplicate variable entries, only the unique ones
are used.
}
\details{
Use "y ~ 0" to specify a stand alone LHS.
}
\note{
This likely won't work for interaction terms (such as \code{*} or \code{:}).
}
\examples{
# Combine everything (default)
combine_formulas(observed ~ rainfall + temp, observed ~ rainfall + forest.cover)
# Combine only LHS
combine_formulas(observed ~ rainfall + temp, observed ~ rainfall + forest.cover, combine = "lhs")
}
\keyword{utils}
