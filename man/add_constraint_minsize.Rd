% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_constraint.R
\name{add_constraint_minsize}
\alias{add_constraint_minsize}
\title{Adds a size constraint on a scenario}
\usage{
\S4method{add_constraint_minsize}{BiodiversityScenario,numeric,character,logical}(mod,value,unit,establishment_step)
}
\arguments{
\item{value}{A \code{\link{numeric}} value describing the minimum amount of area of a
given patch}

\item{unit}{A \code{\link{character}} of the unit of area. Options available are
\code{km2} (Default) and \code{ha}.}

\item{establishment_step}{A \code{\link{logical}} flag indicating whether a given patch
is only to be removed if wasn't small in a previous time step (not yet
implemented!)}
}
\description{
This function applies a minimum size constraint on a \code{scenario()} created
object. The rationale here is that for a given species isolated habitat patches
smaller than a given size might not be viable / unrealistic for a species
to establish a (long-term) presence.

The idea thus is to apply a constraint in that only patches bigger than a
certain size are retained between timesteps.
It has thus the potential to reduce subsequent colonizations of neighbouring
patches.
}
\details{
Area values in a specific unit need to be supplied.
}
\note{
\emph{This function requires that a scenario has a set \code{threshold()}!}
}
\examples{
\dontrun{
scenario(fit) |>
 add_predictors(future_covariates) |>
 threshold() |>
 add_constraint_minsize(value = 1000, unit = "km2") |>
 project()
}

}
\seealso{
Other constraint: 
\code{\link{add_constraint_adaptability}()},
\code{\link{add_constraint_boundary}()},
\code{\link{add_constraint_connectivity}()},
\code{\link{add_constraint_dispersal}()},
\code{\link{add_constraint}()}
}
\concept{constraint}
\keyword{scenario}
