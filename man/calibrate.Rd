% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{calibrate}
\alias{calibrate}
\alias{calibrate,DistributionModel-method}
\title{Calibrate a fitted engine again given a set of parameters and data}
\usage{
calibrate(
  mod,
  point = NULL,
  field_occurrence = "observed",
  project = FALSE,
  verbose = getOption("ibis.setupmessages", default = TRUE),
  ...
)

\S4method{calibrate}{DistributionModel}(
  mod,
  point = NULL,
  field_occurrence = "observed",
  project = FALSE,
  verbose = getOption("ibis.setupmessages", default = TRUE),
  ...
)
}
\arguments{
\item{mod}{A \code{\link{BiodiversityDistribution}} object.}

\item{point}{A \code{\link[sf:sf]{sf::sf}} object containing observational data used for model training.}

\item{field_occurrence}{A \code{\link{character}} location of biodiversity point records (Default: \code{'observed'}).}

\item{project}{A \code{\link{logical}} statement on whether to make a new projection (Default: \code{FALSE}).}

\item{verbose}{A \code{\link{logical}} on whether be chatty. Default option from ibis-wide options.}

\item{...}{Any other parameters not directly referenced.}
}
\value{
Add a new model to a \code{\link{BiodiversityDistribution}} object.
}
\description{
This function (re-)calibrates a model in an engine to find the one that best
matches the data. Data in this context can be either a parameter range
or a new of observational records against which the model should be calibrated
against. See details for more information.
}
\details{
The function \code{calibrate()} is used to re-calibrate a fitted model in an engine.
This is often also known as "tuning" a model. The function takes a fitted model object
and a set of parameters, and uses the data to find the best set of parameters.
}
\examples{
\dontrun{
 mod <- distribution(background) |>
 add_predictors(mypredictors) |>
 add_biodiversity_poipa(species) |>
 engine_glm()

 # Now calibrate again
 mod_better <- calibrate(mod)
}

}
\references{
\itemize{
\item Sillero, N., Arenas-Castro, S., Enriquez‐Urzelai, U., Vale, C. G., Sousa-Guedes, D., Martínez-Freiría, F., ... & Barbosa, A. M. (2021). Want to model a species niche? A step-by-step guideline on correlative ecological niche modelling. Ecological Modelling, 456, 109671.
}
}
\keyword{tuning}
