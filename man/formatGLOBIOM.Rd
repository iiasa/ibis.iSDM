% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_predictors_globiom.R
\name{formatGLOBIOM}
\alias{formatGLOBIOM}
\title{Function to format a prepared GLOBIOM netCDF file for use in Ibis.iSDM}
\usage{
formatGLOBIOM(
  fname,
  oftype = "raster",
  ignore = NULL,
  period = "all",
  template = NULL,
  verbose = getOption("ibis.setupmessages")
)
}
\arguments{
\item{fname}{A filename in \code{\link{character}} pointing to a GLOBIOM output in netCDF format.}

\item{oftype}{A \code{\link{character}} denoting the output type (Default: \code{'raster'}).}

\item{ignore}{A \code{\link{vector}} of variables to be ignored (Default: \code{NULL}).}

\item{period}{A \code{\link{character}} limiting the period to be returned from the formatted data.
Options include \code{"reference"} for the first entry, \code{"projection"} for all entries but the first,
and \code{"all"} for all entries (Default: \code{"reference"}).}

\item{template}{An optional \code{\link{SpatRaster}} object towards which projects should be transformed.}

\item{verbose}{\code{\link{logical}} on whether to be chatty.}
}
\value{
A \code{\link{SpatRaster}} stack with the formatted GLOBIOM predictors.
}
\description{
This function expects a downscaled GLOBIOM output as created in the BIOCLIMA project.
Likely of little use for anyone outside IIASA.
}
\examples{
\dontrun{
# Expects a filename pointing to a netCDF file.
covariates <- formatGLOBIOM(fname)
}
}
\keyword{internal,}
\keyword{utils}
